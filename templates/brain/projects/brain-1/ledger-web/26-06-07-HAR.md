# ledger-web — 26-06-07  (detailed log)

## b21e0d4 — Migrate transactions view to ledger-api /v2

**What changed:** Switched the transactions table to the cursor-paginated
`/v2/transactions` endpoint and added infinite scroll. Kept a temporary `/v1`
fallback shim behind the `useV1Transactions` flag so we can roll back without a
redeploy.

**Why:** Downstream of the `ledger-api` change the same day (7f3a9c1). `/v1` is
being sunset in 90 days, so the client had to move before then; the shim de-risks
the cutover.

**Files:** `src/api/transactions.ts`, `src/views/Transactions.tsx`,
`src/flags.ts`

**Cross-repo impact:** Depends on `ledger-api` 7f3a9c1 (same brain). The shim
and the `useV1Transactions` flag can be deleted once ledger-api completes its
`/v1` sunset.

**Flags / to-dos:** Remove the `/v1` fallback shim post-sunset; drop the flag.
