# weather-cli — 26-06-05  (detailed log)

## 4c8d1f2 — Add --units flag (metric/imperial)

**What changed:** Added a `--units metric|imperial` flag. Defaults to the system
locale and falls back to metric when the locale is unknown or unset.

**Why:** US users kept getting Celsius with no way to switch, and scripting
around it required parsing the human-readable output.

**Files:** `src/cli.ts`, `src/format.ts`, `tests/units.test.ts`

**Cross-repo impact:** None (standalone brain).

**Flags / to-dos:** None.
