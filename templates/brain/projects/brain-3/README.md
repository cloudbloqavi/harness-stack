# brain-3 — markdown-linter (standalone)

> **Scenario: a second unrelated repo, a second brain.** `markdown-linter` is
> unrelated to `brain-1` (Ledger) and to `brain-2` (weather-cli), so it gets its
> own numbered brain. The numbering (`brain-2`, `brain-3`, …) is how multiple
> independent brains coexist under one `projects/` folder.

**Repos in this brain:**
- [`markdown-linter/`](markdown-linter) — standalone linter (no related repos)

**Brain compact:** [`26-06-03-HAR-compact.md`](26-06-03-HAR-compact.md) — the
single daily rollup for this brain.
